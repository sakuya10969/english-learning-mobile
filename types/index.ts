export interface HomeSectionCardProps {
  title: string;
  description: string;
  link: string;
  index?: number;
};

export interface GrammarSectionCardProps {
  title: string;
  description: string;
  link: string;
  index: number;
}
